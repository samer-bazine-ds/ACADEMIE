import {FormEvent,useState} from 'react';import {Mail,Phone,Plus,Search,UsersRound} from 'lucide-react';import {Avatar,Badge,Button,Card,Field,Modal,Select} from '../components/ui';import {useAcademy} from '../lib/store';
export default function Teachers(){const {teachers,modules,levels,students,addTeacher}=useAcademy();const [open,setOpen]=useState(false),[search,setSearch]=useState('');const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);addTeacher(String(f.get('module')),String(f.get('name')),String(f.get('phone')),String(f.get('email')));setOpen(false)};const visible=teachers.filter(t=>t.name.toLowerCase().includes(search.toLowerCase()));return <div className="mx-auto max-w-7xl"><div className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 className="font-display text-2xl font-bold">Enseignants</h2><p className="mt-1 text-sm text-stone-500">{teachers.length} enseignants actifs dans votre école.</p></div><Button onClick={()=>setOpen(true)}><Plus size={17}/>Nouvel enseignant</Button></div><div className="mb-5 flex max-w-md items-center gap-2 rounded-xl border bg-white px-3 py-2.5 text-stone-400"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Rechercher un enseignant…" className="w-full bg-transparent text-sm outline-none"/></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{visible.map(t=>{const m=modules.find(x=>x.id===t.moduleId);const l=levels.find(x=>x.id===m?.levelId);return <Card key={t.id} className="p-5"><div className="flex items-center gap-4"><Avatar name={t.name} size="lg"/><div><h3 className="font-display font-bold">{t.name}</h3><Badge tone="blue">{m?.name||'Module'} · {l?.name}</Badge></div></div><div className="mt-5 space-y-2.5 text-sm text-stone-500"><p className="flex items-center gap-2"><Phone size={15}/>{t.phone||'Non renseigné'}</p><p className="flex items-center gap-2"><Mail size={15}/>{t.email||'Non renseigné'}</p></div><div className="mt-5 rounded-xl bg-stone-50 p-3 text-center"><b>{students.filter(s=>s.teacherId===t.id).length}</b><p className="text-xs text-stone-400">Élèves</p></div></Card>})}</div><Modal open={open} onClose={()=>setOpen(false)} title="Nouvel enseignant"><form onSubmit={submit} className="grid gap-4"><Field name="name" label="Nom complet" required/><Select name="module" label="Module et niveau">{modules.map(m=><option value={m.id} key={m.id}>{m.name} — {levels.find(l=>l.id===m.levelId)?.name}</option>)}</Select><div className="grid grid-cols-2 gap-3"><Field name="phone" label="Téléphone"/><Field name="email" label="E-mail" type="email"/></div><Button type="submit"><UsersRound size={16}/>Ajouter l'enseignant</Button></form></Modal></div>}
