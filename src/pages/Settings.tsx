import {FormEvent,useState} from 'react';import {Building2,Check,Save} from 'lucide-react';import {Button,Card,Field} from '../components/ui';import {useAcademy} from '../lib/store';
const key='academie-school-profile';
export default function SettingsPage(){const {schoolName,registerSchool}=useAcademy();const saved=JSON.parse(localStorage.getItem(key)||'{}');const [done,setDone]=useState(false);const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget),profile={name:String(f.get('name')),phone:String(f.get('phone')),email:String(f.get('email')),address:String(f.get('address'))};registerSchool(profile.name);localStorage.setItem(key,JSON.stringify(profile));setDone(true);setTimeout(()=>setDone(false),2500)};return <div className="mx-auto max-w-3xl"><div className="mb-6"><h2 className="font-display text-2xl font-bold">Paramètres</h2><p className="mt-1 text-sm text-stone-500">Informations visibles dans votre espace d’administration.</p></div><Card className="overflow-hidden"><div className="flex items-center gap-3 border-b p-5 sm:p-6"><span className="grid h-11 w-11 place-items-center rounded-xl bg-brand-50 text-brand-700"><Building2 size={21}/></span><div><h3 className="font-display font-bold">Établissement</h3><p className="text-sm text-stone-400">Identité et coordonnées</p></div></div><form onSubmit={submit} className="grid gap-4 p-5 sm:grid-cols-2 sm:p-6"><Field name="name" label="Nom de l’école" defaultValue={saved.name||schoolName} required/><Field name="phone" label="Téléphone" defaultValue={saved.phone||''}/><Field name="email" label="E-mail" type="email" defaultValue={saved.email||''}/><Field name="address" label="Adresse" defaultValue={saved.address||''}/><div className="flex items-center gap-3 sm:col-span-2"><Button type="submit"><Save size={16}/>Enregistrer</Button>{done&&<span className="flex items-center gap-1 text-sm font-semibold text-emerald-600"><Check size={16}/>Modifications enregistrées</span>}</div></form></Card></div>}
